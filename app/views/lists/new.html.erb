<h1>LIST NEW</h1>

Create using existing:
<%= form_for @list do |f| %>

  <%= f.label :description %>
  <%= f.text_field :description %>
  <br>
  <%= f.label :location %>
  <%= f.text_field :location %>
  <br>
  <%= f.label :activity_id %>
  <%= f.collection_select :activity_id, Activity.all, :id, :category %>


  <%= f.submit %>
<% end %>
